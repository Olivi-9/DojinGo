version: "3.9"
services:
  Dojingo:
    build: .
    container_name: Dojingo
    restart: always
    network_mode: "host"
    environment:
      CONFIG_FILE: "/app/config.yaml"
      TZ: Asia/Singapore
    volumes:
      - "./config.yaml:/app/config.yaml:ro"
      - "./cache:/app/cache"
